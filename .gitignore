# API keys / user secrets — never commit
gateway/.env
last_settings.json

# Runtime output
logs/

# PyInstaller build output — never commit these
dist/
build/
*.spec.bak

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
